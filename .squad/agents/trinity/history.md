# Project Context

- **Owner:** Jose Moreno
- **Project:** net-lab-builder — build, document, and tear down ephemeral Azure Networking labs
- **Stack:** Azure networking (VNet, NSG, UDR, peering, gateways, firewall, Private Link, DNS, BGP); Megaport ExpressRoute; cloud-init NVA templates (NAT, BIRD, StrongSwan+BIRD)
- **Created:** 2026-05-28
- **Role:** Azure Network SME — own addressing, NSG, UDR, peering, gateways, firewall, Private Link, DNS, BGP; three-layer route collection (gateway + circuit + MCR); ER & Megaport gotchas

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

📌 2026-05-28 — Project initialized. Charter integrated with azure-lab skill networking reference. ER labs MUST capture routes at gateway + circuit + MCR (with VXC-resource fallback for empty looking-glass). Megaport quirks: nested `associatedVxcs` under MCR; MCR uses `contractTerm`, VXC uses `term`; never include `config: {}` in MCR payload; never manually create Azure peering when Megaport handles the VXC.

📌 2026-05-29 — Vault Stewardship backfill for lab `expressroute-megaport-bgp` (Phase 3.4 close gate). Created: `Labs/2026-05-ExpressRoute-Megaport-BGP.md`, `Services/Megaport.md`. Appended to: `Services/ExpressRoute.md` (list-route-tables false-negative gotcha + personal labs), `Topics/BGP-on-Azure.md` (custom BGP community non-surfacing via VNet API + personal labs). Updated `_Index.md` Recently-added and date. Five anomalies documented; zero forbidden GUID matches confirmed across all 5 touched files.

📌 2026-06-15 — Phase 3.1 design spec for lab `vwan-dual-er-symmetric` (dual-region secured vWAN, two ER circuits each via its own Megaport MCR to one GCP on-prem; goal: enforce traffic symmetry for the in-hub Azure Firewall). **Symmetry mechanism recommended: A — per-region prefix affinity at MCR** (each MCR's GCP-bound BGP session uses a `megaport_mcr_prefix_filter_list` to advertise only its region's Azure prefixes; Azure→on-prem direction is already per-region because each Hub ER GW prefers its own circuit; the filter alone forces return-path symmetry). Mechanism B (AS-PATH prepend at MCR — private ASN OK because prepend doesn't cross MSEE) included as Scenario 4 fallback with auto-failover trade-off. Mechanisms C (Global Reach) and D (community-driven action) rejected as out-of-scope. ASN map: hub firewalls 65520 (vWAN reserved, same in both hubs), MCR1=65001, MCR2=65002, GCP CR=65003, ER private peering /30s Megaport-managed. Routing intent `private`, hub-routing-preference default (`ExpressRoute`), no Global Reach, no bow-tie, no custom RTs in v1. Three-layer route-collection plan totals **46 captures** (10 L1 Azure CP, 12 L1b vWAN inbound/outbound REST, 8 L2 Megaport, 12 L3 VM/NIC/OS, 4 GCP). **KV finding (correction to lab #1):** `platform-secrets-1138` is NOT private-link-only as lab #1 §8 claimed — it's RBAC-mode, public-network-access-Enabled with default-deny network ACLs and a single allowlisted IP for Jose's dev machine. Three secrets present (all readable): `megaport-api-key`, `megaport-api-secret`, `default-password` (latter added 2026-06-15 during this dispatch). No GCP creds in this vault — auth uses out-of-band `gcloud application-default login`. Terraform `azurerm_key_vault_secret` data-source pattern is viable for lab #2. **KV access bear-trap — Microsoft Global Secure Access (GSA) + KV firewall:** GSA on the dev machine egresses from a non-allowlisted IP, producing 403 `"Client address is not authorized to access this server"` on `az keyvault secret list/show` and identically on `terraform plan/apply` data-source reads. Two operator-coordinated workarounds documented as standing guidance in design.md: **Path A (preferred)** — Jose pauses GSA briefly, operation retried, Jose re-enables GSA, zero KV resource change. **Path B (when A not possible)** — snapshot `networkAcls` JSON first, then `az keyvault update --default-action Allow`, run op, **immediately** restore to `Deny`, verify post-state matches snapshot. Tank's deploy script must probe with `secret list --maxresults 1`, on 403 stop and surface both paths to Jose, **never silently flip the ACL**. Same hook applies at cleanup. **Vault gaps (backfill at lab close):** no `Patterns/Dual-Region-Secured-vWAN-with-ER.md`, no entry on MCR prefix-filter-list Terraform shape in `Services/Megaport.md`, no `Topics/Traffic-Symmetry-Stateful-Firewall.md`, and **no `Topics/KeyVault-GSA-and-Firewall-ACL.md` capturing the GSA bear-trap + restore-fidelity workflow** (high-value cross-cutting finding — bites every Tank deploy that reads KV secrets, not just this lab). **Routing-intent rollout time:** 10–20 min wait window confirmed in failure-mode notes for Niobe. **vWAN VNet-connection effective-routes REST body:** `virtualWanResourceType="HubVirtualNetworkConnection"` is correct; the `effective_routes_vnetcx` helper at `vwan_2xshub.azcli:320–326` hardcodes `"ExpressRouteConnection"` which is a bug for VNet connections — flagged in design.md Layer-1b table.

---

📌 Team update (2026-06-15): Phase 1 manifest design fan-out complete on 2026-06-15; Jose gate pending.
