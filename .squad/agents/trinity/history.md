**Archived entries:** see \history-archive.md\

# Project Context

- **Owner:** Jose Moreno
- **Project:** net-lab-builder — build, document, and tear down ephemeral Azure Networking labs
- **Stack:** Azure networking (VNet, NSG, UDR, peering, gateways, firewall, Private Link, DNS, BGP); Megaport ExpressRoute; cloud-init NVA templates (NAT, BIRD, StrongSwan+BIRD)
- **Created:** 2026-05-28
- **Role:** Azure Network SME — own addressing, NSG, UDR, peering, gateways, firewall, Private Link, DNS, BGP; three-layer route collection (gateway + circuit + MCR); ER & Megaport gotchas

**📌 SUMMARIZATION NOTE (2026-07-31):** This file has grown to ~17KB. Pre-Phase 3 learnings archived in `history-archive.md`. Active learnings (Phase 3 root-cause analysis, Gate D design) retained below.


📌 2026-08-03T17:25+02:00 — **B2 Review: VpnGw1AZ gateway PIP zones fix (dual-hub lab). APPROVED FOR REVISION AND RESUME.**

**Error:** `VmssVpnGatewayPublicIpsMustHaveZonesConfigured` — Azure control plane now requires gateway PIPs attached to AZ-SKU VPN gateways to have explicit zone declarations. The six gateway PIPs (`pip-gw-hub1-a/b`, `pip-gw-hub2-a/b`, `pip-gw-onprem-a/b`) were created zone-less; zones are immutable on an existing PIP.

**Region AZ support verified (Microsoft Learn — learn.microsoft.com/azure/reliability/availability-zones-region-support, fetched 2026-08-03):**
- `swedencentral` ✅ AZ-supported
- `switzerlandnorth` ✅ AZ-supported
- `norwayeast` ✅ AZ-supported

**Zone configuration confirmed correct:** Microsoft docs (create-zone-redundant-vnet-gateway, fetched 2026-08-03) state: "This example specifies all three regional zones, which creates a zone-redundant public IP address." Specifying `zones: ['1','2','3']` on all three regions is the canonical zone-redundant form and satisfies `VmssVpnGatewayPublicIpsMustHaveZonesConfigured`. (Note: docs also state a zone-less Standard PIP is "equivalently zone-redundant" — but this is **superseded by the platform enforcement** reflected in the actual error. The explicit `['1','2','3']` is the compliant path.)

**Scope of deletion:** Exactly six unattached gateway PIPs. No VPN gateways exist (all three failed to create). ARS PIPs (`pip-ars-hub1`, `pip-ars-hub2`, `pip-ars-poland`) are **not in scope** — they are in separate resource declarations, not referenced by the gateway modules, have no zones requirement for ARS, and must not be deleted.

**IaC delta correctness:** Adding `zones: ['1','2','3']` to each of the six `pipGwHub1A/B`, `pipGwHub2A/B`, `pipGwOnpremA/B` resource declarations in `main.bicep` is minimal, correct, and sufficient. No changes to `vpngw.bicep`, ARS PIPs, or any other resource are required or authorized.

**Authorization:** Tank is authorized to: (1) apply only this six-resource zones delta to `main.bicep`; (2) run `az bicep build` + `az deployment group validate`; (3) confirm the six gateway PIPs are unattached (no VPN GW); (4) delete exactly those six PIP names; (5) resume incremental deployment on existing RG. All prior approvals (Wave 0–2, VpnGw1AZ SKU) carry forward.


📌 2026-08-03T17:04+02:00 — **Re-gate: VPN GW SKU retirement — VpnGw1 → VpnGw1AZ (dual-hub lab).** Deployment partially succeeded (46/49 resources); all 3 VPN GWs failed with `NonAzSkusNotAllowedForVPNGateway`. MS docs confirm creation of VpnGw1-5 blocked effective 2025-11-01 (gateway-sku-consolidation). **Verified VpnGw1AZ in all 4 regions** (swedencentral, switzerlandnorth, norwayeast, polandcentral) via Azure Retail Pricing API — $0.21/hr present in all four. VpnGw1AZ confirmed: BGP ✅, active-active ✅, VNet-to-VNet ✅, ASN 65515 ✅, ARS coexistence ✅, Generation1 ✅. **Pricing correction:** Tank claimed "same pricing tier; unchanged." Inaccurate — VpnGw1AZ = $0.21/hr vs VpnGw1 = $0.19/hr (+10.5%); 6 billing units → +$2.88/day; baseline ~$65.86 → ~$68.74/day. Guardrail already approved-breached — no Jose re-gate required. **Bicep change applied:** `sku: { name: 'VpnGw1AZ', tier: 'VpnGw1AZ' }` in `vpngw.bicep` — minimal, correct, sufficient. **APPROVED FOR REVISION AND RESUME.** Tank authorized to run incremental deploy on existing RG `rg-dual-hub-hubless-region-ars-lab3d001`/correlationId `lab3d001`. All prior phase approvals carry forward.

 — **Owning iteration: workload-aligned third-region ARS (dual-hub lab redesign).** Jose proposed replacing the central-ARS 4th-region VNet with a third-region ARS aligned to third-region workloads (three spoke sets: A→hub1, B→hub2, C→ARS-VNet-in-third-region). Reviewer verdict: FEASIBLE **with one structural correction** — a bare ARS-VNet in the third region has a **data-plane reachability gap** because ARS injects `0/0 → NVA_IP` where NVA_IP is in hub1/hub2 (different VNets, non-transitive peering). Fix A (recommended): route-hub in third region = 1× ARS + **1× small transit NVA** (BIRD+strongSwan, ASN 65003) with IPsec overlays to NVA1/NVA2 — matches multi-region ARS canonical pattern (learn.microsoft.com/azure/route-server/multiregion). Fix B (simpler NVA count, more peerings): each set-C spoke peers to route-hub + hub1 + hub2, with UseRemoteGateways only on the route-hub peering. Confirmed: peering single-flag "**Use the remote virtual network's gateway or Route Server**" exposes BOTH VPN GW transit AND ARS injection when both are present in the hub — one flag, both behaviours (ARS FAQ verbatim). No UDR needed for `0/0` on hub-local spokes. AllowForwardedTraffic required on BOTH ends of every peering that carries NVA-forwarded traffic. Confirmed: third-region ARS BGP-peering NVA1 + NVA2 across global peering is supported — NVAs need multi-hop eBGP, must peer both ARS instance IPs, 16-peer limit (updated from 8), 4000 routes/peer. **Δ1 (65515 strip on all three NVAs' local-ARS-facing sessions) remains MANDATORY** — loop-prevention runs before inbound route-maps in the ARS pipeline. **Δ3 (inbound route-map on third-region ARS ↔ NVA2 for `0/0` prepend) is the default-route lever** for split policy. **Δ3-bis (Phase-0 experiment)**: outbound route-map on hub2 ARS→VPN-GW connection may be able to prepend set-C spoke prefixes (they arrive at hub2 ARS as BGP-learned from NVA2, NOT as VNet address space — the "cannot modify VNet address space" restriction may not apply). Confirm empirically. If it works, retire NVA2-side prepend in favour of route-map placement — tightens the "which lever lives where" pedagogical story. If ambiguous, keep NVA-side. **Fourth-region SPOF from Δ1–Δ4 is now removed** — each region's control plane is co-located with its workloads. Cost story: Fix A is materially cheaper than a full hub (no VPN GW, no firewall) — real scaling unit is "one route-hub per workload region, not per spoke." Route-maps preview: incurs extra charges (per pricing page, no exact figure to cite), first route-map on an ARS triggers ~30 min upgrade — one-off, must schedule at Phase-0. **Baseline VM count: 4 spokes / 3 workload VMs** (set-A×1, set-B×1, set-C-spoke-1×1, set-C-spoke-2 prefix-only). Set-C-spoke-2 without a VM proves "one ARS per workload region scales *within* a region" at zero incremental compute cost. Failure injection for hub1 outage: both-PSK-break on hub1↔on-prem VPN connection pair + `sudo systemctl stop bird` on NVA1 — REVERSIBLE, no VM lifecycle. F5 (route-hub NVA single-VM failure in third region) is the known SPOF in v1 — P1 patch (second route-hub NVA) closes it if Jose accepts the extra small-VM cost. Decision inbox: `.squad/decisions/inbox/trinity-third-region-ars-design.md` (full walk, peering table, ASN plan, 5 pass/fail scenarios, D1/D2/D3 for Jose). All source references: route-server-faq, multiregion, about-dual-homed-network, expressroute-vpn-support, route-maps-about, virtual-network-peering-overview, vpn-gateway-bgp-overview — all fetched 2026-08-03. No `design.md`, manifest, or IaC touched.


📌 2026-08-03T11:51+02:00 — **Follow-up review pass for Jose (dual-hub central-ARS lab): 6 fix/workaround questions on the fourth-region SPOF + wrong-PSK injection viability.** Jose accepted the SPOF-scoping and chose split route-policy (route-map for default preference, NVA-side BGP AS-path prepend for spoke prefixes) + two spokes (one per hub region). Findings vs Microsoft Learn:
(1) **Dual central-ARS active/standby is a dead-end for a *single* spoke.** Peering constraint "Only one peering can have this flag set to true" (learn.microsoft.com/azure/virtual-network/virtual-network-peering-overview → Gateways/on-premises + AVM `remotePeeringUseRemoteGateways` doc) means a spoke can point to **exactly one** remote-gateway/remote-Route-Server VNet. Automation that flips peerings on outage: for each spoke, delete both peering halves, recreate with the new remote gateway target, wait ~30–120 s per spoke for propagation, then spoke re-learns routes from the new central ARS. RTO is O(minutes) per spoke, not seconds; effective routes reset (all peering-derived NHs churn); this is worse than the 180 s ARS hold ceiling we already accept. REJECT for this lab.
(2) **Regional shared UDR with 0/0 flipped between NVA1↔NVA2 IPs** is technically feasible: a route table is region-scoped but can be associated with subnets in multiple VNets in the same region (same or cross-subscription with perms). Cross-region next-hop = NVA IP works over global VNet peering (only the *Basic* LB frontend restriction remains across global peering per the peering overview doc; NVA IP as NH is supported). Downsides: forward path becomes fully automation-driven (no BGP feedback — a silent NVA hang won't flip the UDR), return path stays BGP → asymmetric-by-construction; scaling adds one UDR per (region, next-hop-family). This is a legitimate simplification over central ARS but abandons the "route-injection via ARS" learning goal. Log as **Follow-up-Lab candidate**, not this-lab substitute.
(3) **Production-canonical (per-hub ARS + NVA overlay + app/DNS failover)** is Morpheus's original alternative — it removes the fourth-region SPOF and matches learn.microsoft.com/azure/route-server/multiregion. But it *removes* the VPN-GW/ARS coexistence + central-ARS + route-maps-preview teaching targets. Correct outcome: separate follow-up lab.
(4) **Native Azure "single-spoke cross-region active/standby NH without another regional dependency" — does not exist.** vWAN routing intent: each vHub is regional; spoke has one hub-attachment. AVNM Routing Configuration (learn.microsoft.com/azure/virtual-network-manager/how-to-create-udr-network-manager) automates UDR deploy across a network group but is a *management-plane* solution over the same regional UDR primitive from (2) — data-plane behavior identical. Cross-region Global LB is inbound-VIP only (TCP/UDP frontend), never a UDR next-hop. Gateway LB is regional (VXLAN-chained to a same-region NVA fleet). Route Server is regional. **Verdict: no native primitive removes the regional-anchor requirement — every option pins one leg to a region.**
(5) **Route-maps split policy for THIS design:** ✅ inbound map on central-ARS→NVA2 peering for NVA2-originated 0/0 (prepend ×N) affects best-path (route-maps preview doc: "Outbound route maps modify route advertisements only and don't influence Azure Route Server's best-path selection, because path selection happens before outbound route maps are applied" → inverse: inbound is pre-best-path). For hub2 local-ARS: spoke prefixes re-originated by NVA2 into hub2 ARS **are BGP-learned** (from NVA's perspective, they are eBGP routes from central ARS), so they *are* route-map-eligible on the NVA→hub2-ARS peering direction. **But**: outbound route-map on hub2 VPN-GW *connection* toward on-prem is **not supported on classic Azure VPN Gateway** — connection-level route-maps exist only on Virtual WAN vHubs (learn.microsoft.com/azure/virtual-wan/route-maps-about). Classic VPN GW has no per-connection AS-path prepend / route-map primitive. Verdict: **NVA-side BGP filter (BIRD/FRR) is unavoidable for the spoke-prefix prepend leg** — even though NVA2 could in theory do only the 65515-strip and let route-maps do the prepend, the prepend cannot land on classic VPN GW connection. Do both: route-map for default (Δ3 unchanged) + NVA-side prepend for spoke prefixes (Δ2 unchanged).
(6) **Wrong-PSK injection for hub1 outage simulation — VIABLE and clean.** In a VNet-to-VNet dual-AA topology, break the connection object(s) `conn-hub1-to-onprem` and `conn-onprem-to-hub1` (bidirectional pair; change PSK on **both** with `az network vpn-connection shared-key update`, then `az network vpn-connection reset` to force immediate IKE renegotiation instead of waiting for rekey/DPD). Behavior: IKE Phase-1 auth fails → tunnel drops → BGP session over tunnel drops → hub1-learned routes withdrawn on on-prem within BGP hold-time (~30 s to ~180 s depending on TCP RST vs silent). Recovery = restore original PSK (keep in KeyVault-backed script variable) + reset connection — idempotent. **Pair with `sudo systemctl stop bird` on NVA1** to also kill NVA1↔central-ARS peering → forward-path 0/0 withdrawn from spokes via central ARS best-path shift to NVA2 → **full hub1 regional-outage simulation without touching any Azure resource lifecycle** (no VM stop/dealloc, no VNet delete). This is the cleanest reversible failure injection for the lab.

**Ranked recommendation for THIS lab (unchanged goal, minimal delta from Δ1–Δ3):** KEEP central ARS + split route policy + 2 spokes. Add Δ4 (wrong-PSK + BIRD-stop injection recipe). DO NOT attempt dual central-ARS or per-spoke peering flip (option 1). DO NOT try connection-level route-map on classic VPN GW (Q5 blocker). Log options 2/3 as follow-up-lab candidates. All source references: peering-overview, route-server-faq, route-maps-about (ARS + vWAN), multiregion (ARS), vpn-gateway-bgp-overview, virtual-network-manager UDR docs — all fetched 2026-08-03. No lab files or `design.md` touched. Reviewer response delivered to Jose as plain-text summary; Morpheus artefact not revised (per Jose instruction).


📌 2026-08-17T11:53:31.285+02:00 — **Team update (foundry-agent-reserved-prefix-reachability): Network design locked; blocking corrections applied to manifest.md.**
- **C1:** VpnGw1 → VpnGw1AZ (Azure blocked VpnGw1 creation 2025-11-01). Cost delta: +$0.96/day. Lab baseline still within $50/day guardrail.
- **C2:** Dual-NIC confounder eliminated — replaced with two single-NIC VMs (vm-onprem-echo at 172.30.100.4, vm-onprem-ctrl at 10.200.100.4). Ensures S3/S4 comparison depends only on destination prefix variable. Cost delta: +$0.27/day.
- **C3:** DNS Private Resolver subnet gap closed — added dedicated DNSInboundSubnet (192.168.3.0/28) and DNSOutboundSubnet (192.168.3.16/28) to vnet-foundry plan. Applies only if S5 in scope.
- **Design file:** `labs/foundry-agent-reserved-prefix-reachability/design.md` now authoritative for networking specification. Manifest.md ready for Phase 0 preflight and Jose's Phase 4 approval. — *Decided by Trinity*

---

<!-- Append new learnings below. Each entry is something lasting about the project. -->


📌 2026-08-05T10:32:00+02:00 — **Route-map user stories guide created for dual-hub-hubless-region-ars.** File: `labs/dual-hub-hubless-region-ars/route-map-user-stories.md`. Key design-decision findings codified:
(1) **Four-mechanism model** (route visibility A, next-hop selection B, traffic authorization C, topology D) is the correct mental model before applying any routing policy. Route maps operate only on A (BGP advertisement control plane). They do not substitute for NSG/firewall (C) or create topology (D).
(2) **ARS route map eligibility rule** (same-VNet constraint): only BGP peers whose peerIp is within the ARS VNet CIDR are eligible for route-map attachment. This is the root cause of EMP-001 (ars-poland cross-VNet NVA peers rejected). ars-hub1/ars-hub2 NVA peers ARE eligible (NVA1/NVA2 are in their respective hub VNets). ARS VPN GW and ER GW connections are always eligible (co-located in hub VNet).
(3) **"Advertise prefixes only to the NVA subnet" is not achievable via route maps** — this conflates route-table visibility (route maps can help) with traffic containment (NSG/firewall required) with topology (peering/tunnel required). All three mechanisms are needed. Route maps can filter what NVA re-advertises but cannot prevent VMs from sending traffic if a route exists.
(4) **Strongest inter-hub recommendation**: do not add hub1↔hub2 direct peering without UDRs forcing NVA chaining on both sides. An NVA-to-NVA IPsec overlay is the production-canonical inter-hub path (matches learn.microsoft.com/azure/route-server/multiregion). Route maps can then filter which prefixes are visible on the overlay BGP session.
(5) **Priority order for remaining route-map experiments** (verified eligible attachment points): Story H community tagging (ars-hub1/hub2 inbound NVA peering) → Story E on-prem filter (ars-hub1/hub2 inbound VPN GW) → Story D filter infra prefixes (outbound VPN GW) → Story G summarization (requires address redesign first). Each of ars-hub1 and ars-hub2 first-use upgrades trigger ~30 min outage + ~$6/day surcharge (irreversible).
(6) **BIRD/FRR is superior to route maps for cross-VNet scenarios** (no locality constraint, no surcharge, no upgrade, more expressive). Route maps add value when: the peer is local, the team wants GUI observability, or ARS-native community/summarization is needed downstream.
(7) **10.31/24 + 10.32/24 cannot be safely summarized** — smallest common aggregate is 10.0.0.0/10. Production spoke CIDRs must be allocated from a contiguous block (e.g., 10.32.0.0/20 for Poland region).
(8) **Summarization strips BGP Community and AS-PATH** (platform rule, confirmed in MS Learn). If AS-PATH preference (Δ2) or community tagging (Story H) must survive, use BIRD aggregate instead of route-map Replace action.
Decisions inbox: `.squad/decisions/inbox/trinity-route-map-user-stories.md`.


📌 2026-08-03T14:27+02:00 — **design.md created for dual-hub-hubless-region-ars (Fix-B triple-peering model, no overlay).** Written from manifest.md, decision inbox entries (trinity-third-region-ars-design, trinity-dual-hub-design-review-reviewer-pass), and ARS Learn refs. Key design choices codified: (1) Fix-B selected — set-C spokes triple-peered to Poland ARS VNet (`UseRemoteGateways`) + hub1 + hub2 (data-plane-only, no transit flags); two data-plane peerings give fabric path to NVA1_IP and NVA2_IP without any overlay. (2) Δ1: both NVAs strip 65515 outbound to local hub ARS (BIRD `bgp_path.delete(65515)`). (3) Δ2: NVA2 prepends 65002×2 on set-C prefixes to hub2 ARS (NVA-side only — classic VPN GW has no per-connection route-map). (4) Δ3: inbound ARS route-map on Poland ARS↔NVA2 peering prepends with RFC5398 doc-ASN 64496 (not 65002 — private range forbidden in ARS route-map actions) × 2; flagged as U1 (unproven, validate in S4). (5) 9 Standard PIPs verified: 3×ARS + 3×VPN GW AA×2. (6) 4-object V2V connection model (no LNG) validated as documented behavior. (7) PSK reset + BIRD stop = full hub1 outage simulation; convergence 0–180s. (8) Resiliency table F1–F8; P1 dormant (ILB NVA pair in hub1/hub2, +~$0.56/day per hub). (9) U1–U4 unproven-behavior flags with fallbacks. File: `labs/dual-hub-hubless-region-ars/design.md` (≤20 KB).

--- Corrected verdict: **feasible as teaching-lab** with three minimal deltas that PRESERVE Azure VPN GWs + central ARS + route-maps preview scope. Key findings validated against Microsoft Learn: (1) NVA stripping ASN 65515 from AS_PATH before advertising to local hub ARS is the canonical multi-region-ARS pattern — explicitly documented on https://learn.microsoft.com/azure/route-server/multiregion, not a "silent-blackhole risk"; BIRD `bgp_path.delete(65515)` or FRR `set as-path exclude 65515` one-liner. (2) Route-maps preview genuinely cannot modify VNet address-space advertisements (correct fact per https://learn.microsoft.com/azure/route-server/route-maps-about) — but that's not the preference lever; NVA2 prepend on spoke prefixes to local hub2 ARS is preserved intact (FAQ: "AS path preserved intact"), then preserved end-to-end over Azure VPN Gateway (only ExpressRoute strips private ASNs to 12076; VPN does not). (3) Two hub VPN GWs sharing ASN 65515 to on-prem VPN GW is fine — eBGP peers with same remote ASN are identified by IP; best-path picks shorter AS_PATH, which is exactly the lever. (4) ARS route-maps CAN be usefully tested via inbound map on NVA2 peering for NVA2-originated 0/0 — route-maps preview supports modifying NVA-originated default, and inbound maps ARE applied before ARS best-path (per doc "Outbound route maps modify route advertisements only and don't influence Azure Route Server's best-path selection"). (5) 180s ARS hold is a real ceiling but overstated by Morpheus — real convergence is seconds when TCP RST is emitted; 180s only in silent-failure brownout. (6) Central-ARS-region SPOF is real; acceptable if scoped explicitly, patch P3 duplicates ARS as documented production evolution. **Response written to .squad/decisions/inbox/trinity-dual-hub-design-review-reviewer-pass.md** with AS-path walk table (spoke prefix → on-prem via hub1: `[65515, 65001]`; via hub2 with prepend ×3: `[65515, 65002, 65002, 65002]`), 6-row failure-mode table, 3-patch catalogue (P1 NVA-side aggressive KEEPALIVE, P2 UDR secondary default, P3 dual-region ARS), and answers to all four of Morpheus's gate questions from a networking-technical viewpoint. Morpheus NOT re-assigned to revise; final scope call is Jose's. Reference set: route-maps-about, route-server-faq, multiregion, vpn-gateway-bgp-overview — all fetched 2026-08-03.

---


📌 2026-08-03T16:22+02:00 — **IaC review: dual-hub-hubless-region-ars — REJECTED.** Reviewed Tank's Bicep/cloud-init/PowerShell against the locked `design.md` contract. Found one blocking defect:
**B1 — `nsg-nva-hub1` missing `allow-bgp-multihop-inbound` rule for `10.30.0.0/24` (Poland ARS, TCP/179).** NVA1's BGP sessions #1 and #2 (NVA1↔ars-poland, design §4) would be denied by the deny-all fallback at priority 4000. NVA2's NSG correctly includes this rule at priority 105. Without it, Poland ARS never learns routes from NVA1, set-C spokes get no 0/0→NVA1 injection, and the entire topology (S1–S5) fails. All other IaC items verified correct: 20 peering objects with exact flags, 3 AA VpnGw1 gateways, 9 PIPs, V2V connections with correct GW IDs and shared PSK pairs, BIRD Δ1/Δ2 filters, UDRs, no NSG on GatewaySubnet/RouteServerSubnet. Non-blocking observations: manifest peering count says 13 pairs/26 objects vs actual 10/20; manifest says `multihop 3` vs BIRD's correct `multihop 4`; ARS module BCP073 lint warning on `sku` property. Verdict recorded in `.squad/decisions/inbox/trinity-dual-hub-iac-review.md`. Tank is under reviewer lockout — fix to `nsgNvaHub1` in `main.bicep` must be authored by a different eligible agent.

---

📌 Team update (2026-07-31T15:35:00Z): **VWAN-ROUTEMAP-SUMMARIZATION LAB FULLY DECOMMISSIONED.** All three clouds torn down: Tank deleted Azure RG routemap-test-rg (39 min, ResourceGroupNotFound verified); Link decommissioned all Megaport products (2 MCRs + 3 VXCs via CANCEL_NOW API) and confirmed GCP project vwan-routemap-lab in DELETE_REQUESTED (billing stopped); Trinity finalized README with complete teardown-status table (all rows ✅ DONE) and lab completion banner "✅ LAB FULLY DECOMMISSIONED". Lab lifecycle: 2026-06-15 through 2026-07-31 (~6 weeks). Total cost: ~$4,200. Evidence preserved in show-output/ (52 files across all phases). No ongoing costs. Documentation finalized. Lab ready for publication and archive. All decisions merged to .squad/decisions.md. Orchestration logs + session log created for team memory. **End of lab lifecycle.**

📌 Team update (2026-08-05T10:26:52.618+02:00): Route-map user-story design decisions merged; four-mechanism model and ARS eligibility rule captured.


📌 2026-08-05T13:58+02:00 — **Storage endpoint observable-path equivalence design completed.** Created `labs/storage-endpoint-path-equivalence/design.md` from Morpheus's locked card. Minimal sequential topology: one VM/subnet with stable NAT egress, target+decoy GPv2 accounts, target Blob PE at `10.61.2.4`, controlled `privatelink.blob.core.windows.net` VNet link, target-only service endpoint policy, VNet flow logs, and Blob resource logs. Comparison authority is DNS + fresh-connection PCAP destination + NIC effective next hop + Storage `CallerIpAddress`/authorization; flow logs corroborate and traceroute is explicitly rejected. Isolation detail: pre-stage the target VNet rule while Storage default action is Allow, so S1→S2 changes only `Microsoft.Storage`; S4 leaves SE/policy in place and changes only private-DNS linkage. Five executable gates cover public, SE, endpoint-policy target/decoy, PE, and public-network-disabled negative control. Current Learn behavior checked 2026-08-05. Phase 4 remains closed; no Azure writes/IaC. Team decisions: `.squad/decisions/inbox/trinity-service-endpoint-path.md`.

---


📌 2026-08-05T13:30+02:00 — **US12 revision authored after Niobe's rejection (Morpheus locked out).** Scope: US12 body + front index/summary/matrix/recommended-order cells, US11 test-1 endpoint sentence, and one stale count in `manifest.md`. **Blocking fixes:** (1) **Global Reach confined to side S-D and outcome B1** — removed from every outcome-C / local-ER-failover claim; it links on-premises networks (`azure/expressroute/expressroute-global-reach`) and carries no prefix across S-B, so it cannot restore hub1 workload reachability through hub2. (2) **Global Reach shared failure domain documented** — it rides *both* ER private peerings: usable when the failure is the ER gateway or above a live circuit, lost when the local circuit/private peering/peering-location is the failure; also its routes count against the private-peering budget (ER FAQ). (3) **No-overlay B2 probe re-anchored** — verified from `main.bicep` + `manifest.md` + `deploy-log.md` that **no VM exists in either hub `snet-endpoint`** (10.10.2.0/27, 10.20.2.0/27 exist but are empty) and that `vm-hub1-ep`/`vm-hub2-ep` are spoke-resident (10.11.0.x / 10.21.0.x), so the drafted probe was false and would have tested spoke reachability across a non-transitive peering. Re-anchored on **`vm-nva1` 10.10.1.4 ↔ `vm-nva2` 10.20.1.4** with prerequisites N1–N8: NSG already allows ICMP + TCP/22 from 10.0.0.0/8 (no NSG change), `deny-other-inbound` p4000 forbids arbitrary-port probes and TCP/179 must never be probed, BIRD strictly read-only, route-refresh capability confirmed before the peering create, host-terminated traffic so no `AllowForwardedTraffic` dependency and no transit claim. Costed fallback (2 hub endpoint VMs, 6→8) documented but not recommended. **Non-blocking:** ARS ER↔ER citation scope-limited to *same virtual network gateway* (square anchored instead on MSEE **AS 12076** + *"You can't configure ExpressRoute as transit routers"*, `azure/expressroute/expressroute-routing`, and private-ASN stripping per `azure/virtual-wan/route-maps-prepend-routes`); ER circuit-to-circuit through ARS reclassified **`Platform-blocked — retained`**; route-map cells carry *eligible but unassociated; gateway-connection attachment unverified*; normal-state diagram no longer ticks B2 unconditionally; diagram specs rewritten for **Mermaid** (`flowchart TB`, four quadrant subgraphs, side letters in edge labels, absent diagonals as a `NOT PRESENT — by design` annotation node rather than red-crossed geometry, draw.io/Excalidraw only as an optional supplement). **manifest.md:** peering count corrected **26 objects/13 pairs → 20 objects/10 pairs** (evidence: §3 matrix = 10 pairs; `main.bicep` = 20 `virtualNetworkPeerings` resources) — same defect I flagged non-blocking in the 2026-08-03 IaC review, now fixed. Nothing deleted; no other story body touched; no diagrams, no IaC, no commit. Note recorded in `.squad/decisions/inbox/trinity-us12-revision.md`.

---


📌 2026-08-05T13:43:07.691+02:00 — Scribe merge pass: controlled storage endpoint path-equivalence design brief recorded in decisions.md; design.md stays unstaged; Phase 4 remains closed.

---


📌 2026-08-05T19:45+02:00 — **Storage endpoint deployment blocker root cause corrected.** Live Policy Insights, Activity Log, Resource Change History, and Defender configuration inspection prove the public-access control is management-group assignment `MCAPSGovDeployPolicies`, custom definition `StorageAccount_PublicNetwork_Modify`, effect `modify`; it rewrites Storage `publicNetworkAccess` to `Disabled`. The later `DefenderForStorageSecurityOperator` writes added the Defender data-scanner resource-access rule and onboarded Defender/Event Grid, but did not change public access. Selected-networks mode cannot work because it still requires PNA Enabled. NSP is policy-compliant but changes the authorization model and invalidates the approved SE comparison. Recommended path is a security-owner-created, ≤12-hour exemption scoped only to target+decoy, with OAuth/private-container/NSG controls retained and immediate restoration afterward. No Azure changes or cleanup performed; tests remain blocked. Analysis: `labs/storage-endpoint-path-equivalence/blocker-analysis.md`; decision inbox: `.squad/decisions/inbox/trinity-storage-sepath-policy-blocker-analysis.md`.

---


📌 2026-08-05T21:30+02:00 — **Phase-4 Stage-1 activation plan finalized for the bow-tie/regional-affinity evaluation (`labs/dual-hub-interconnect-ars-route-policy`), read-only; nothing mutated, nothing committed.** Live reconciliation after the Poland cleanup (29/29 objects deleted; zero resources remain in polandcentral; RG `rg-dual-hub-hubless-region-ars-lab3d001` now 50 resources) shows the bed is **quiescent, not merely idle**: all five VMs are `VM deallocated` (all `Standard_B2ts_v2`, correcting the manifest's SKU row), so both Route Servers return empty learned **and** advertised sets on both instances, `vpngw-hub1 list-advertised-routes --peer 10.40.0.4` is empty, `vpngw-onprem` learns no hub or spoke prefix, the hub gateways show the ARS instances (10.10.0.68/.69, 10.20.0.68/.69) in `Connecting`, and the spoke `0.0.0.0/0` UDRs (`rt-spoke-a`→10.10.1.4, `rt-spoke-b`→10.20.1.4) are black-holed. Confirmed live that **no hub1↔hub2 peering exists** (only `peer-hub1-to-spoke-a` and `peer-hub2-to-spoke-b`, both gwt=T), that both `bgpConnections` carry `routingConfiguration` with **only** `vnetRoutes` (no map anywhere), that `rm-hub1-activate`/`rm-hub2-activate` are `Succeeded` with `associatedInbound/OutboundConnections: []` but are **not empty** (one `rule-activate-synthetic`: match `192.0.2.0/24` Equals → Add asPath 64496 → Terminate), and that all four `Microsoft.Network/connections` are `Connected`, 4 tunnels, `routingConfiguration: {}`. **API-semantics correction (this supersedes the PATCH wording everywhere in the lab):** the association write target is the **connection** — `virtualHubs/<ars>/bgpConnections/<peer>` → `properties.routingConfiguration.inboundRouteMap.id` — the verb is **PUT, not PATCH** (this child type defines no PATCH operation; expect 405) and the body must carry `peerAsn` + `peerIp` in full; `routeMaps/*.associatedInboundConnections` is a **read-only composite**, confirmed both by live GET and by `New-AzRouteMap`/`Update-AzRouteMap` exposing no `-InboundConnection`/`-OutboundConnection` parameter — **the lab's Δ3 finding is correct and must not be regressed**. Also fixed two real script defects: T2b targeted `virtualHubRouteTables/...` (wrong resource type) and T2a used `--method patch`. **Plan:** U0 start `vm-nva1`+`vm-nva2` only (**+$0.024/hr = +$0.58/day** retail; **not** routing-neutral — BIRD re-originates `0/0` and the RouteServerSubnet `/27`, un-black-holing the spoke UDRs); U1 exactly two peering objects (vna=T, fwd=T, gwt=F, urg=F; $0/hr, ≈$0.00 data, headline evidence is a *non*-effect); U2 inert association on **`ars-hub1`/`peer-nva1` only** using a **dedicated temporary map `rm-hub1-tmp-assoc`** (not `rm-hub1-activate` — it is not empty and its tier-activation provenance must survive; no repeat of the ~30-min upgrade, already done on both hubs); U3 real behaviour on **`10.10.0.64/27`** with `Add asPath [64496,64496]` (0/0, 10.10.0.0/16 and 10.11.0.0/24 all ruled out); U4 **Step 1 read-only only** — T5 may be **unverifiable**, since no ARS↔VPN-gateway connection object exists in this model; U5 **not preapproved** — it would additionally require **TCP/179 NSG rules on both `nsg-nva-hub1` and `nsg-nva-hub2`** (today 179 is allowed only from the local hub /16, deny-all at 4000) plus BIRD mutation. **Recommended tranche: U0 + U1 only** (+$0.58/day, ≈45–60 min, <10 min rollback); U1 and U2 must not share a window. **Scope honesty recorded as a first-class finding: this bed cannot prove regional affinity.** The bow-tie needs two on-prem sites; there is one (`vnet-onprem`, AS 65000) dual-homed to both hubs, so Stage 1 delivers only a **single-site dual-homed hub-preference proxy** and the bow-tie verdict stays *not determined* until `vnet-onprem2` + `vpngw-onprem2` (`VpnGw1AZ` ≈ **$5.04/day**) exist behind gate G3. Two controls are permanently gone: both `bird.conf` files still point `ars_poland_0/1` at the deleted `10.30.0.4/.5` (they will sit in Connect/Active forever — expected, not a failure), the Poland/set-C prefixes can never reappear, and the **Δ2 signature `65002-65002-65002` is retired** as a "must not move" control in favour of a fresh post-U0 baseline. BIRD **route-refresh capability was never captured anywhere** (`show protocols all` exists in no lab) — documentary expectation only, and it is the explicit gate on U1; BIRD configs are hand-edited on the OS disks and are **not** in version control. Wrote `.squad/decisions/inbox/trinity-bowtie-activation-plan.md` and updated the lab's `README.md`, `manifest.md`, `design.md` (§8a), `validation.md` (§U0 evidence set), `deploy-log.md` (Phase-4 unit ledger + G1/G4 notes), both gated scripts and all six body placeholders — every Azure call still commented out.

---


📌 2026-08-06T16:00+02:00 — **Phase-4 refinement after finding TANK-001: prerequisite unit U1.5 defined, U2 corrected against the real API model, U3 re-designed around a safe prefix (`labs/dual-hub-interconnect-ars-route-policy`). Read-only inspection only; no Azure or NVA state mutated, no association made, nothing committed.** U0/U1 passed on 2026-08-06 and Tank's evidence surfaced **TANK-001**: both NVAs still carry retired Poland BIRD state — rs_poland_0/1 stuck in `Connect`, their `export_to_poland_ars` filters, a stray static `route 10.30.0.0/27`, and on nva2 a dead `10.31.0.0/24`/`10.32.0.0/24` prepend clause (the retired Δ2 signature). **Root cause established:** `export_to_hub_ars`/`export_to_hub2_ars` filter by **AS-PATH** (`bgp_path.delete(65515)`), **never by prefix**, so every `protocol static` route is re-originated into the local Route Server by construction; `10.30.0.0/27` is therefore a genuine eBGP route at both ARS (asPath 65001/65002), contained (absent from ARS's advertised-back set, both hub gateways and `vpngw-onprem`; visible only in each NVA's own NIC effective routes) and confirmed still present by live read-only capture. **Chasing it invalidated a second, quieter assumption and that is the more valuable finding: `10.10.0.64/27` — the previously-chosen U3 target — is in `bird.conf` and exported identically to every other static, yet never appears in ars-hub1's learned set on either instance, with no error surfaced anywhere. Azure Route Server silently rejects a route matching its own RouteServerSubnet.** A map keyed on it would have provisioned `Succeeded` and matched nothing: U3 would have *read* as a PASS while proving nothing at all. Logged as **TANK-001a**, with the operational rule that no route-map test target may be selected before confirming it appears in `list-learned-routes`. **Third finding, TRIN-001:** live GET shows `bgpConnections/peer-nva1` carries `routingConfiguration.vnetRoutes.staticRoutesConfig` = `{propagateStaticRoutes: true, vnetLocalRouteOverrideCriteria: "Contains"}`; since this child type has **no PATCH** and `PUT` replaces `properties` wholesale, the lab's existing U2 placeholder bodies — which omitted `vnetRoutes` entirely — would have silently reset both settings to service defaults, a real routing-behaviour change with **no U2 criterion that would have caught it**. Adopted rule: **the PUT body is derived from a fresh GET, never authored** — take `properties`, delete only `provisioningState`, add `inboundRouteMap.id`, send with `If-Match: <etag>`, body from file. **U1.5 defined** as a `$0`, no-Azure-object prerequisite removing exactly the four (nva1) and five (nva2) statements above, quoted from the captured config using BIRD's real protocol names rather than guessed ones: backup + sha256, stage to `bird.conf.u15`, **blocking** dual syntax gate (`bird -p -c` *and* `birdc configure check`), then **`birdc configure`** — graceful, reconfigures unchanged protocols in place so `ars_hub*` sessions do not flap — with **`systemctl restart bird` explicitly forbidden** (it would black-hole both spokes' `0.0.0.0/0`). Expected delta is **exactly one prefix**: `10.30.0.0/27` withdrawn from both instances of both Route Servers, with gateways/on-prem byte-identical as non-regression; eight-layer proof matrix, eight rollback triggers, `birdc configure undo` or file restore in **under 2 min per NVA**, nva1 first as the second NVA acts as an unchanged control. Sequencing decided: **U1.5 and U2 may share one approval tranche** (both `$0`, non-overlapping blast radii — one NVA-side, one ARM-side, both reversible in minutes) but **must not share a maintenance window**, because U1.5's whole purpose is to *change* the learned-route set while U2's headline criterion is that it *did not change*; baselines B0 (post-U1, exists) → B1 (post-U1.5) → B2 (post-U2) → B3 (post-U3a) are explicit. **U3 unblocked but split**: U3a injects RFC 5737 TEST-NET-2 `198.51.100.0/24` into nva1's BIRD as a **blackhole** static — BGP-visible, incapable of carrying traffic — because no already-advertised prefix is safe (`0.0.0.0/0` = spoke UDR target/DEF-001; `10.40.0.0/16` = on-prem and present on IN_1 only, so any diff is unfalsifiable; `10.30.0.0/27` is removed by U1.5); U3b then applies `Add asPath [64496,64496]`. The three RFC 5737 blocks are deliberately kept distinct — `192.0.2.0/24` for the pre-existing `rm-hub1-activate`, `203.0.113.0/24` for U2's inert rule (**changed from 192.0.2.0/24**, which would have made any effect ambiguous between two maps on the same Route Server), `198.51.100.0/24` for U3 — so no evidence is ever ambiguous about which artefact produced it. `64496` re-verified as the RFC 5398 2-byte documentation ASN: not private, not Azure-reserved, same block as the `64511` in Microsoft's own prepend walkthrough. **Honest caveat declared in advance rather than rationalised afterwards:** it is not established whether `list-learned-routes` reports AS-PATH post-map or as-received, so if the map reports `Succeeded` and the change is invisible in both CLI and portal, U3b is **INCONCLUSIVE — tooling visibility, not FAIL** (U2 remains the association proof), and retrying against a production prefix to force visibility is forbidden. Created `nva-config/` (as-found snapshots for both NVAs — closing the standing "hand-edited, not in version control" gap and providing a second restore source — U1.5 targets, the U3a snippet, and the full remediation contract) plus evidence-placeholder READMEs for `u15-bird-cleanup` and `u3a-doc-prefix`; updated `manifest.md`, `design.md` §8a(d-correction), `validation.md` (new §U1.5, T2 and T2b rewritten), `README.md`, `deploy-log.md`, `lessons-learned.md`, and both gated scripts (new `U15`/`U3a` scenarios, corrected `T2a`/`T2b`) — **every Azure call still commented out**. Approval summary written to `.squad/decisions/inbox/trinity-u15-u2-refinement.md`: recommended tranche **U1.5 + U2, one approval, two windows**, `$0` incremental cost against an unchanged +$0.58/day run-rate, ≈45–70 min wall clock, <5 min rollback per unit; U3a/U3b a separate approval after U2 passes; U4 Step 2 and U5 explicitly excluded.

---


📌 2026-08-14T08:30+02:00 — **Network design completed for foundry-agent-reserved-prefix-reachability lab.** Authored `labs/foundry-agent-reserved-prefix-reachability/design.md` from Morpheus's locked Stage-1 manifest plus Foundry networking deep-dive (fetched live). AzureNetworking vault [[Topics/Hybrid-DNS]] and [[Services/VPN-Gateway]] consulted.

**Three blocking corrections applied to manifest.md (surgical, not scope-expanding):**

**(C1) VpnGw1 SKU blocked** — same root cause as dual-hub lab (2026-08-03 learning). Corrected to VpnGw1AZ ($0.21/hr × 2 = $10.08/day). Lab still well within $50/day guardrail.

**(C2) Dual-NIC confounder eliminated** — original design placed primary NIC (172.30.100.4, WorkloadSubnet) and secondary NIC (10.200.100.4, CtrlSubnet) on a single VM. Return traffic for connections arriving at the secondary IP would default-route via the primary NIC unless Linux policy routing (`ip rule add from 10.200.100.4 table N`) is explicitly configured. This confounds S3 vs S4 comparison (destination prefix is not the only variable). Replaced with two single-NIC VMs (vm-onprem-echo + vm-onprem-ctrl), each with one default gateway and deterministic symmetric return path. One extra B2ts_v2 (~$0.27/day). Same resource count model: 3 VMs total.

**(C3) DNS Private Resolver subnet gap** — Private Resolver inbound and outbound endpoints require dedicated /28 subnets with `Microsoft.Network/dnsResolvers` delegation; cannot share PESubnet. Added DNSInboundSubnet 192.168.3.0/28 and DNSOutboundSubnet 192.168.3.16/28 to vnet-foundry subnet plan. Applies only if S5 is in scope.

**Key design findings (design.md):**
- Deep-dive confirms data proxy (AgentSubnet, 192.168.0.0/24) is the correct origin point for agent tool call probes — prompt agent tool calls transit the single-tenant data proxy, not the client-side HTTP path.
- Limitation scope confirmed from live doc fetch (2026-08-14): "address spaces of your VNET… and peered VNETs" — no mention of VPN-learned routes. H₁ is expected but outcome is explicitly unknown.
- Return-path: vpngw-foundry auto-advertises 192.168.0.0/16 to vpngw-onprem; gateway propagation on vnet-onprem subnets is mandatory for symmetric return.
- DNS chain for echo.onprem.lab: data proxy → 168.63.129.16 → DNS Private Resolver outbound EP → forwarding ruleset (onprem.lab → 172.30.100.4:53) → dnsmasq on vm-onprem-echo → A 172.30.100.4.
- HTTP (port 80) recommended as primary test protocol to eliminate TLS certificate validation as a confounder.
- Container Apps NSG requirements for AgentSubnet specified; AzureLoadBalancer + VirtualNetwork inbound + selective outbound rules.

**Decision inbox:** `.squad/decisions/inbox/trinity-foundry-reserved-prefix-design.md` — full correction rationale for Jose review.

**Hypothesis state preserved as unknown.** No Azure resources created.


