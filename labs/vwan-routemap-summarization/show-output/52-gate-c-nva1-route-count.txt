# az vm run-command invoke -g routemap-test-rg -n nva1 --command-id RunShellScript --scripts "sudo birdc show route count" --query "value[0].message" -o tsv
# Gate C — nva1 (hub-eu1, swedencentral) BIRD route count
# hub-eu1 state: Routing Intent PrivateTraffic ENABLED (since Gate B)
# Captured: 2026-07-31T11:45:00+02:00
# Subscription: <SUBSCRIPTION_ID>
# RG: routemap-test-rg

Enable succeeded: 
[stdout]
BIRD 1.6.8 ready.
37 of 37 routes for 27 networks

[stderr]

# Identical to Gate A and Gate B counts (37/27).
# RI on BOTH hubs did NOT change the route count on nva1.
# The NVA RIB is driven by BGP from the hub VPN GW and is independent of the hub's RI forwarding table.
